apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: s2i-dotnet-pipeline-run-8.0-ubi8
spec:
  pipelineRef:
    name: s2i-dotnet-pipeline
  params:
  - name: VERSION
    value: 8.0-ubi8
  - name: EXAMPLE_REVISION
    value: dotnet-8.0
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc
